GEMINI_API_KEY=
GOOGLE_DRIVE_FOLDER_ID=
PORT=3000
REFRESH_INTERVAL_MS=3600000

# Drive access — use a service account for reliable bulk loads (no rate-limit wall).
# Paste the entire service-account JSON key on one line:
GOOGLE_SERVICE_ACCOUNT_JSON=
# Fallback only (rate-limited): an API key works for small vaults.
GOOGLE_DRIVE_API_KEY=
